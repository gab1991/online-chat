import React from 'react';
import Avatar from '../UI/Avatar/Avatar';
import styles from './UserSettings.module.scss';

export default function UserSettings() {
  return <Avatar text={'bam'} size={150} />;
}
